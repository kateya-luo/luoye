# 安全策略

## 报告安全问题

请不要在公开 Issue 中提交密码、访问令牌、设备密钥、录音、转录内容、数据库或完整 `.env`。仓库上线后，建议启用 GitHub Private Vulnerability Reporting，并通过私密渠道提供复现步骤和受影响版本。

## 禁止提交的内容

- `.env`、私钥、证书私钥和真实 API 密钥
- 设备生产凭据、Wi-Fi 凭据和烧录后的 NVS 镜像
- 用户录音、字幕、导出报告和运行数据库
- 带真实账户、内网地址或未脱敏日志的诊断资料
- 云模型供应商密钥及备份文件

根目录的 `.gitignore` 和 `scripts/repo_audit.ps1` 只提供基础防线，不能代替人工审查和 GitHub Secret Scanning。

## 部署要求

工程环境可在受控局域网调试；面向公网时必须使用 HTTPS、独立随机密钥、最小权限账户、访问速率限制和可靠备份。固件中的工程服务器地址也必须在生产发布前重新审查。
