map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 100m;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /health {
        proxy_pass http://server:8000/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_connect_timeout 5s;
        proxy_read_timeout 10s;
    }

    location = /health/ready {
        proxy_pass http://server:8000/health/ready;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_connect_timeout 5s;
        proxy_read_timeout 10s;
    }

    location /ws/ {
        proxy_pass http://server:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
        proxy_buffering off;
    }

    location /api/ {
        proxy_pass http://server:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_connect_timeout 10s;
        # ESP32 通过公网发送 10 MiB 范围时，SD 读取、Wi-Fi 抖动或蜂窝上行可能让
        # 两次正文数据之间停顿超过 Nginx 默认 60 秒。保留 1 小时可恢复上传窗口。
        client_body_timeout 3600s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

}

# HTTPS 预留：取得证书后配置 443 server 块，并将 80 重定向到 HTTPS。
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name recorder.example.com;
#     ssl_certificate     /etc/letsencrypt/live/recorder.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/recorder.example.com/privkey.pem;
#     # 将上方 server 块中的 location 配置复制到这里。
# }
