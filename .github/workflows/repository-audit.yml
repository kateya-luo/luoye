name: Repository audit

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  audit:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check repository contents
        shell: powershell
        run: ./scripts/repo_audit.ps1
